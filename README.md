# [ gradle + spring + react + docker] boilerplate

## BUILD
``
.\gradlew clean build docker
``

## RUN
``
docker-compose -f ./deploy/docker-compose.yml up
``

App should be served on http://localhost:8080/ 
